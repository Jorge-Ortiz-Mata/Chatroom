<%= turbo_frame_tag 'rooms_controller' do %>
  <main class="flex w-full h-screen justify-center">
    <nav class="w-1/6 flex flex-col">
      <%= render 'shared/nav_bar' %>
    </nav>

    <section class="w-5/6 flex flex-col items-center justify-center gap-5 h-screen">
      <h1 class="font-bold text-4xl">Bienvenido - Chat App</h1>
      <p class="text-center font-semibold">This is a simple Chatroom for Rails App.</p>
    </section>
  </main>
<% end %>
